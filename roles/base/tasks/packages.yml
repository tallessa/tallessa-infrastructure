---
- name: Update apt cache
  apt: update_cache=yes cache_valid_time=3600

- name: install basic packages
  apt: >
    pkg={{ item }}
    state=present
  with_items:
    - screen
    - vim
    - git
    - htop
    - wget
    - python3-venv
    - python3-dev
    - build-essential
